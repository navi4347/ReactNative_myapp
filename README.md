# Welcome to your Expo Application

This project was bootstrapped using `create-expo-app`, then structured for scalable development inside the `src/` directory.  
The `app/` directory (Expo Router) remains **untouched** and optional.

---

## Create a New Expo App (Optional, Fresh Start)

If you are starting a new project:

```bash
npx create-expo-app@latest my-app
cd my-app
pnpm install
pnpm expo start
